{
  "type": "object",
  "properties": {
    "service": {
      "type": "object",
      "description": "DC/OS service specifications of YOUR APP",
      "properties": {
        "name": {
          "description": "Name of YOUR APP instances as seen in DC/OS.",
          "type": "string",
          "default": ""
        },
        "cpus": {
          "description": "Number of logical cores to allocate to each service instance.",
          "type": "number",
          "default": 0.3
        },
        "mem": {
          "description": "Memory in Mb to allocate to each service instance.",
          "type": "number",
          "default": 2048
        },
        "disk": {
          "description": "Number of disks to allocate to each service instance.",
          "type": "number",
          "default": 0
        },
        "instances": {
          "description": "Number of instances of your service.",
          "type": "number",
          "default": 1.0
        },
        "cmd": {
          "description": "A shell command for your container to execute.",
          "type": "string",
          "default": ""
        }
      }
    },
    "networking": {
      "type": "object",
      "description": "Configure the networking for your service.",
      "properties": {
        "network_type": {
          "description": "Choose a container type. Refer to the ports documentation for more information.",
          "type": "string",
          "enum": [
            "host",
            "bridge"
          ],
          "default": "bridge"
        },
        "container_port": {
          "description": "Container port number",
          "type": "number",
          "default": 8080
        }
      }
    },
    "environment": {
      "type": "object",
      "description": "Configure any environment values to be attached to each instance that is launched.",
      "properties": {
        "ENV_VAR": {
          "description": "Any variable",
          "type": "string",
          "default": ""
        }
      }
    }
  }
}